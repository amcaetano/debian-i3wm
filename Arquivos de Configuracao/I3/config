############################################################################
################### Arquivo de Configuração do i3wm (v4) ###################

# Link para acesso a documentação do i3wm https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

#------- Fonte usada no i3 bar ----------#
font pango:monospace 16

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# Lightdm iniciando com o sistema
exec --no-startup-id lightDm

# Configuração para iniciar o papel de parede do nitrogen

exec --no-startup-id nitrogen --restore 

# Iniciar resolucao de tela adequada
exec --no-startup-id xrandr -s 1920x1080

# Remover beep quando teclas são apertadas por engano ao iniciar o sistema
exec --no-startup-id xset b off

# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# Lançar terminal
bindsym $mod+Return exec i3-sensible-terminal

# Fechar janela ativa
bindsym $mod+q kill

# Lançar e configurar cores do dmenu
# Legenda de terminologias ==> -nb (cor de fundo), -nf (cor da fonte), -sb (cor da seleção de opções mostradas), -sf (cor da fonte opção selecionada), -fn (estilo da fonte usada) 
bindsym $mod+d exec --no-startup-id dmenu_run -i -nb '#191919' -nf '#25D366'  -sb '#6EAA5E' -sf '#191919' -fn 'NotoMonoRegular:bold:pixelsize=17'



# navegação janelas ativas
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+shiftl+focus up
bindsym $mod+ccedilla focus right

# Navegação alternativa janelas ativas
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+ccedilla move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+ctrl+a focus parent

# Definição de números de áreas de trbalho virtuais
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# navegação de áreas de trabalho virtuais
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Mover aplicativos abertos de uma área de trbalho para outra.
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# recarregar parâmetros do arquivo de configuração do i3.
bindsym $mod+Shift+c reload
# recarregar i3wm
bindsym $mod+Shift+r restart
# Lançar opções de desligar ou reiniciar sistema i3wm.
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym ccedilla resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if avaiGGlable)
bar {
        status_command i3status
        position top

#cores da i3bar
# As cores mais relevantes são "statusline" e "focused_workspace"
#---------- Opções de cores para customizar ----------#
# Green Light ==> #25D366
# Purple ==> #A020f0

        colors {
            background #000000
            statusline #25D366
            separator #666666
           # Colorclass                   Border    Backgr.    Text
           focused_workspace             #F9FAF9    #25D366   #000000
           active_workspace              #a9a9a9    #5f676a   #eeeeee
           inactive_workspace            #a9a9a9    #222222   #a9a9a9
           urgent_workspace              #a9a9a9    #d42121   #FFFFFF

          }  

    }
# Reiniciar computador
bindsym $mod+ctrl+r exec systemctl reboot

# Desligar computador
bindsym $mod+ctrl+d exec systemctl poweroff

# Bloquear tela do computador
bindsym $mod+ctrl+b exec i3lock -c 000000

# Abrir navegador web
bindsym $mod+n exec brave-browser

# Abrir editor de texto mousepad
bindsym $mod+m exec mousepad

# Abrir gerenciador de arquivos
bindsym $mod+a exec pcmanfm

# Abrir localizador de aplicativos appfinder
bindsym $mod+ctrl+f exec xfce4-appfinder

# Abrir gimp
bindsym $mod+g exec gimp

# Abrir o localsend
bindsym $mod+l exec localsend

#bindsym $mod+Print --no-startup-id exec scrot 'captura_tela-%Y-%m-%d-%s_$wx$h.jpg' -e 'mv $f $$(xdg-user-dir PICTURES)'
bindsym Print exec scrot

# Ocultar borda com titulo
for_window [class="^.*"] border pixel 1

# Cor de borda da janela ativa
new_window 1pixel
client.focused #25D366 #25D366 #25D366 #25D366   #25D366

# Ocultar barra do i3status permanentemente
bindsym $mod+b bar mode toggle









